/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./views/**/*.ejs"],
  darkMode: 'class', // or 'media' if you prefer the media query approach
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}